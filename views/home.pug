extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in videos
            +videoBlock({
                videoFile:item.videoFile,
                title:item.title,
                description:item.description,
                views:item.views

            })
