extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in videos
            +videoBlock({
                id:item.id,
                videoFile:item.videoFile,
                title:item.title,
                description:item.description,
                views:item.views

            })
