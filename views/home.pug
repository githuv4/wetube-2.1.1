extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in videos
            +videoBlock({
                id:item.id,
                fileUrl:item.fileUrl,
                title:item.title,
                description:item.description,
                views:item.views
            })
