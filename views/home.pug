extends layouts/main
include mixins/videoBlock

block content
    //- .pageTitle=pageTitle 
    .pageTitle 
        span.loggedUserName=userName
        span=userText
    .videos
        each video in videos
            +videoBlock3({
                id:video.id,
                fileUrl:video.fileUrl || video.link,
                youtubeLink:video.youtubeLink,
                title:video.title,
                creatorId:video.creator.id,
                createName:video.creator.name
            })
    

                //- description:video.description,
                //- views:video.views
        

