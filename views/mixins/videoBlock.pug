mixin videoBlock(video={})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoFile controls=true)=video.videoFile
        h4.videoBlock__title=video.title
        h5.videoBlock__description=video.description
        h6.videoBlock__views=video.views

    